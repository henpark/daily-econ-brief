# 처음부터 끝까지 · 30단계

## 먼저, 왜 더블클릭해도 아무 일이 없었나

제가 드린 `.py` 파일들은 **맥에서 실행하는 프로그램이 아닙니다.** 인터넷 어딘가에 있는 컴퓨터가 읽고 실행할 **작업 지시서**입니다.

그림으로 보면 이렇습니다.

```
[Henry님 맥]  ← 아무것도 안 돌아갑니다. 꺼져 있어도 됩니다.
      │
      │ (딱 한 번, 파일을 업로드)
      ▼
[GitHub]  ← 여기가 매일 아침 07:35에 알아서 깨어납니다
      │
      ├─→ Google Gemini 에게 "오늘 시장 조사해서 정리해줘" 요청
      │
      ├─→ 결과를 웹페이지로 발행
      │
      └─→ 07:50에 카카오톡으로 요약 + 링크 발송
                    │
                    ▼
            [Henry님 폰] 📱 띵
```

그래서 지금부터 할 일은 딱 하나입니다.
**GitHub이라는 무료 서비스에 파일을 올려두고, 열쇠 3개를 쥐어주는 것.**

열쇠 3개는 이겁니다.

| 열쇠 | 어디서 받나 | 무슨 용도 |
|---|---|---|
| Gemini API 키 | Google AI Studio | AI에게 조사·작성을 시키는 열쇠 |
| 카카오 REST API 키 | 카카오 개발자 | 카톡을 보낼 수 있는 앱의 신분증 |
| GitHub 토큰(PAT) | GitHub | GitHub이 스스로 설정을 고칠 수 있게 하는 열쇠 |

**터미널(검은 화면)은 한 번도 안 씁니다.** 전부 웹사이트 클릭입니다.
총 30단계, 40분쯤 걸립니다. 중간에 끊고 나중에 이어서 해도 됩니다.

---

# PART 1 · 열쇠 받기 (1~12번)

메모장을 하나 열어두세요. 받은 값을 여기다 붙여넣어 둡니다.

## Gemini 키

**1.** https://aistudio.google.com/apikey 접속 → 구글 계정으로 로그인

**2.** **Create API key** (또는 "API 키 만들기") 버튼 클릭

**3.** 프로젝트를 고르라고 하면 아무거나 선택 → 키가 만들어집니다

**4.** 만들어진 키를 복사해서 메모장에 붙여넣기
> 메모장에 이렇게 적어두세요: `Gemini 키 = AIza...`

**5.** ⚠️ **결제수단(카드)은 절대 등록하지 마세요.**
> 이게 "0원 보장" 장치입니다. 카드가 없으면 구글이 청구할 방법 자체가 없습니다.
> 무료 한도를 넘으면 돈이 나가는 게 아니라, 그날 브리핑이 안 오고 끝납니다.

## 카카오 키

**6.** https://developers.kakao.com 접속 → 카카오 계정으로 로그인

**7.** 화면 맨 위 로고 오른쪽 첫 번째 메뉴 **앱** 클릭 → **애플리케이션 추가하기**
- 앱 이름: `econ-brief` (아무거나 괜찮습니다)
- 사업자명: 본인 이름
- 앱 대표 도메인: `example.com` (`https://` 없이 도메인만)
  > 이 값은 "나에게 보내기"와 아무 상관이 없습니다. 나중에 바꿀 수도 있습니다.
- 저장

> 예전엔 이 메뉴 이름이 "내 애플리케이션"이었는데 지금은 그냥 **앱** 입니다.
>
> 첫 화면에 뜨는 "카카오톡 공유 API 유료 상품 출시" 배너는 무시하세요.
> 저건 **공유 API** 얘기고, 우리가 쓸 건 **나에게 보내기(카카오톡 메시지)** 라 다른 물건입니다.
> 앱 안에서 **유료 API** 설정 화면을 보게 되면 **사용 안 함**으로 두세요.
> 그러면 한도를 넘어도 과금이 아니라 그냥 실패합니다.

**8.** 만들어진 앱을 클릭 → 왼쪽 메뉴 **앱 설정 › 플랫폼** → **Web 플랫폼 등록**
- 사이트 도메인 칸에 `https://example.com` 입력 → 저장

**9.** 왼쪽 메뉴 **제품 설정 › 카카오 로그인** → 활성화 설정을 **ON**

**10.** 왼쪽 메뉴 **앱 설정 › 앱** 의 `∨` 를 펼치기 → **플랫폼 키** → **REST API 키** 영역
→ **리다이렉트 URI** 에 `https://example.com/oauth` 입력 → 저장

> ⚠️ 카카오가 위치를 옮겼습니다. 예전엔 [카카오 로그인 › 일반] 안에 있었는데
> 지금은 **[앱 › 플랫폼 키]** 안에 있습니다. 카카오 로그인 페이지엔 없습니다.
>
> 이 주소가 실제로 열리진 않습니다. 카카오가 "여기로 돌려보낼게" 하는 표지판일 뿐입니다.

**11.** **같은 화면에서** 값 두 개를 복사해 메모장에 붙여넣기
- **REST API 키** → `카카오 키 = 1a2b3c...`
- **클라이언트 시크릿** → `카카오 시크릿 = ...`

> 클라이언트 시크릿은 예전엔 선택이었는데 **지금은 필수**입니다.
> 카카오 문서: "REST API 키는 클라이언트 시크릿 기능이 활성화된 상태로 추가되어,
> 토큰 발급 시 관련 파라미터를 포함해야 합니다."
> 이게 없으면 나중에 토큰 발급이 실패합니다.

**12.** 왼쪽 메뉴 **카카오 로그인 › 동의항목** → 목록에서 **카카오톡 메시지 전송** 찾기
→ 오른쪽 **설정** → **이용 중 동의** 선택 → 저장
> 이걸 안 하면 나중에 `insufficient scopes` 오류가 납니다. 제일 자주 빠뜨리는 단계입니다.

---

# PART 2 · GitHub에 파일 올리기 (13~18번)

**13.** https://github.com 접속 → 계정이 없으면 **Sign up** 으로 무료 가입

**14.** 오른쪽 위 **＋** → **New repository**
- Repository name: `daily-econ-brief`
- **Public 선택** ← 중요합니다
  > 무료로 쓰려면 공개여야 합니다. 비공개는 웹페이지 발행이 유료입니다.
  > 열쇠들은 암호화 금고에 따로 들어가니 공개돼도 안전합니다.
  > 다만 매일 만들어지는 대시보드 페이지는 누구나 볼 수 있습니다. (시황 분석이라 문제없습니다)
- 아래 **Create repository** 클릭

**15.** 다음 화면에서 **uploading an existing file** 링크 클릭
> 안 보이면 주소 뒤에 `/upload/main` 을 붙여서 들어가세요.

**16.** ⚠️ **파일을 올리기 전에** — Finder에서 제가 드린 폴더를 열고
**`Command + Shift + .`** 를 누르세요.
> `.github` 라는 숨김 폴더가 나타납니다. 이게 제일 중요한 폴더인데 맥이 기본으로 숨겨둡니다.

**17.** 폴더 안 **모든 항목**을 GitHub 업로드 창으로 드래그
- `.github` 폴더 (← 이게 반드시 포함돼야 합니다)
- `generate_dashboard.py`, `gemini_client.py`, `send_kakao.py`, `exchange_code.py`
- 나머지 파일들

**18.** 아래 초록색 **Commit changes** 클릭
> 잠시 뒤 파일 목록이 보이면 성공입니다. `.github` 가 목록에 있는지 꼭 확인하세요.

---

# PART 3 · 금고에 열쇠 넣기 (19~24번)

**19.** 레포 상단 **Settings** 탭 → 왼쪽 메뉴 **Secrets and variables** → **Actions**

**20.** **New repository secret** 클릭 → 이름과 값을 넣고 저장. 이걸 세 번 반복합니다.

| Name 칸에 입력 | Secret 칸에 붙여넣기 |
|---|---|
| `GEMINI_API_KEY` | 4번에서 받은 Gemini 키 |
| `KAKAO_REST_API_KEY` | 11번에서 받은 카카오 REST API 키 |
| `KAKAO_CLIENT_SECRET` | 11번에서 받은 클라이언트 시크릿 |

> 이름은 **대문자와 밑줄까지 정확히** 똑같아야 합니다. 오타가 제일 흔한 실패 원인입니다.

**21.** 이제 세 번째 열쇠(GitHub 토큰)를 만듭니다.
새 탭에서 https://github.com/settings/personal-access-tokens/new 접속

**22.** 아래처럼 채웁니다.
- Token name: `econ-brief`
- Expiration: **No expiration** (또는 가장 긴 기간)
- Repository access: **Only select repositories** → `daily-econ-brief` 선택
- Permissions → **Repository permissions** 펼치기 → **Secrets** 항목을 **Read and write** 로 변경
- 맨 아래 **Generate token** → 나온 값 복사

**23.** 레포의 Secrets 화면으로 돌아가 **New repository secret**
- Name: `GH_PAT`
- Secret: 22번에서 복사한 값

**24.** 같은 화면에서 **Variables** 탭 → **New repository variable**
- Name: `DASHBOARD_URL`
- Value: `https://<본인깃허브아이디>.github.io/daily-econ-brief/`
  > 예: 아이디가 `henrypark` 이면 `https://henrypark.github.io/daily-econ-brief/`

---

# PART 4 · 카카오 로그인 한 번 하기 (25~28번)

카카오가 "이 앱이 내 카톡에 메시지 보내도 좋다"는 허락증을 발급하는 과정입니다. 한 번만 하면 됩니다.

**25.** 제가 드린 **`카카오-로그인-링크-만들기.html`** 파일을 더블클릭
> 이 파일은 더블클릭해도 됩니다. 브라우저에서 열립니다.

**26.** 열린 페이지에 11번의 카카오 REST API 키를 붙여넣기
→ **카카오 로그인하러 가기** 버튼이 나타나면 클릭 → 카카오 로그인 → **동의하기**

**27.** 동의하면 **빈 화면이나 오류 페이지**가 뜹니다. **정상입니다.**
이때 **브라우저 주소창을 통째로 복사**하세요.
> `https://example.com/oauth?code=AbCdEf...` 이렇게 생겼습니다.

**28.** GitHub 레포로 돌아가서 **Actions** 탭 →
왼쪽 목록에서 **1회용 · 카카오 토큰 등록** 클릭 →
오른쪽 **Run workflow** 버튼 → 입력칸에 27번 주소를 붙여넣기 → 초록 **Run workflow**

> ⏱️ code는 몇 분이면 만료됩니다. 26~28번은 이어서 빠르게 하세요.
> 만료됐으면 26번부터 다시 하면 됩니다. 몇 번을 다시 해도 괜찮습니다.

1분쯤 뒤 실행 기록을 눌러 **✅ KAKAO_REFRESH_TOKEN Secret 저장 완료** 가 보이면 성공입니다.

---

# PART 5 · 웹페이지 켜고 테스트 (29~30번)

**29.** **Settings** → 왼쪽 메뉴 **Pages**
- Source: **Deploy from a branch**
- Branch: **main** / 폴더는 **/docs** 선택 → **Save**

**30.** **Actions** 탭 → 왼쪽에서 **Daily Economic Dashboard** →
**Run workflow** → 초록 **Run workflow**

5~10분 기다리세요. 카카오톡 **나와의 채팅**에 메시지가 오면 끝입니다. 🎉

이후로는 **매주 월~금 아침 07:50에 자동으로** 옵니다. 맥은 꺼져 있어도 됩니다.

---

# 안 될 때

Actions 탭 → 빨간 ❌ 표시된 실행을 클릭 → 빨간 단계를 펼치면 이유가 한국어로 나옵니다.

| 화면에 보이는 말 | 원인과 해결 |
|---|---|
| `insufficient scopes` | 12번(동의항목 · 카카오톡 메시지 전송)을 안 켰습니다 |
| `invalid_grant` | code가 만료됐거나 이미 썼습니다. 26번부터 다시 |
| `invalid_client` | 카카오 REST API 키가 틀렸거나 Redirect URI가 다릅니다 |
| `GH_PAT Secret이 없습니다` | 23번을 건너뛰었습니다 |
| `모든 모델 실패 ... 404` | Gemini 모델 이름 문제. Variables에 `GEMINI_MODEL` = `gemini-2.5-flash` 추가 |
| `무료 한도 초과(429)` | **과금 아닙니다.** 오늘 한도를 다 썼을 뿐, 내일 정상화됩니다 |
| Pages 주소가 404 | 29번 직후엔 1~2분 걸립니다. 조금 기다리세요 |
| `.github` 를 못 찾겠다 | 16번 `Command + Shift + .` 을 다시 누르세요 |

막히면 빨간 단계의 글자를 그대로 복사해서 저에게 보여주세요. 어디가 문제인지 짚어드리겠습니다.

---

# 나중에 알아두면 좋은 것

- **내용 바꾸기**: `generate_dashboard.py` 파일의 `PASS1` 부분이 AI에게 주는 주문서입니다. 관심 종목이나 산업을 구체적으로 적어두면 결과가 훨씬 좋아집니다. GitHub 웹에서 연필 아이콘으로 바로 고칠 수 있습니다.
- **시간 바꾸기**: `.github/workflows/daily-dashboard.yml` 의 `cron` 부분입니다.
- **주말에도 받기**: 같은 파일의 `0-4` 를 `*` 로 바꾸세요.
- **믿을 만한가**: AI가 쓴 글입니다. 대시보드 맨 아래 **🔎 팩트체크 노트**에 뭘 고쳤고 뭐가 확인 안 됐는지 적혀 있습니다. 매매 전에 그걸 먼저 보세요.
